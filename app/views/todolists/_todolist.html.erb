
    <%= content_tag_for :li, todolist do %>
    <span><%= todolist.name.capitalize %></span>
    <%= link_to 'Edit', edit_todolist_path(todolist), :class=>'edit' %>
    <%= link_to 'Destroy', todolist, confirm: 'Are you sure?', :remote=>true,method: :delete, :class=>"destroy" %>
<span class="handle">[drag]</span>
  <% end %>